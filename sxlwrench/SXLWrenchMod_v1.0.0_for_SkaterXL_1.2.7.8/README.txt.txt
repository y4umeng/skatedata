Send this parent folder and all it's contents to a .ZIP and then install like any other mod using whatever UMM version

Notes
UMM version should not matter. However, if it conflits with any other mods UMM requirements you can attempt to change the UMM version in the Info.json file then re-install